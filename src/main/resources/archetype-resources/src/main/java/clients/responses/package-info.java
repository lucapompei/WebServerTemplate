#set($symbol_pound='#')
#set($symbol_dollar='$')
#set($symbol_escape='\')
package ${package}.clients.responses;

/*
 * This package contains all the models for the responses served at the client layer.
 */