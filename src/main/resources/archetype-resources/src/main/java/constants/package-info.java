#set($symbol_pound='#')
#set($symbol_dollar='$')
#set($symbol_escape='\')
package ${package}.constants;

/*
 * This package contains all the utility classes used to store the constants value like endpoint constants.
 * All the members of a constants class should be a static final field and this kind of class can't be instantiated.
 */