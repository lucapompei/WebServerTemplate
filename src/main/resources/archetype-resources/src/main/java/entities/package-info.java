#set($symbol_pound='#')
#set($symbol_dollar='$')
#set($symbol_escape='\')
package ${package}.entities;

/*
 * This package contains all the models of the entities stored on the database. This package, with the package repositories,
 * is used to implement the persistance layer.
 */